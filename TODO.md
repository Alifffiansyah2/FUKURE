# TODO: Integrate RotatingText

- [ ] Ensure dependency for `motion/react` exists (install `motion`).
- [ ] Fix TypeScript implicit `any` errors in `src/components/ui/RotatingText.tsx`.
- [ ] Run `npm run lint` and `npm run build` to verify.

